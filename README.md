# purpose of this repo

- This is going to serve as the front-end part to my
  blog-api project. I already have the express REST api made,
  so now I just need to start designing the front-end part.

- note: still need to enable express.js CORS so that the front-end
  and the back-end can talk to each other and make requests.

## made a npm package for global CSS reset

- Made and published a custom npm package for a CSS
  reset so that I don't have to manually define beginning CSS for projects
  anymore from scratch.
- Made a free organization under my npm account called "christiantran"
  and published the CSS reset file under @christiantran/utility-css-reset.

- Imported the CSS reset file under the main.jsx to apply to the entire app.
